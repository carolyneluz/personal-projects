<h1><%= @genre.name %></h1>

<h2>Filmes deste Gênero:</h2>
<ul>
  <% @movies.each do |movie| %>
    <li><%= link_to movie.title, movie_path(movie) %></li>
  <% end %>
</ul>

<%= link_to 'Editar Gênero', edit_genre_path(@genre) %> |
<%= link_to 'Todos os Gêneros', genres_path %>